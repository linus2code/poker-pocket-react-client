import React from 'react';
import SettingsBar from '@/components/game/SettingsBar';

const Footer = () => {
  return (
    // <!-- Copyright & badge -->
    <div className="container" style={{ minWidth: '850px' }}>
      <SettingsBar />
    </div>
  );
};

export default Footer;
